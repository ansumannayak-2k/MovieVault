{
  "name": "movievault",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "MovieVault — Search movies via OMDb and manage a Watchlist (HTML/CSS/JS).",
  "scripts": {
    "dev": "serve . -p 5173",
    "test": "NODE_OPTIONS=--experimental-vm-modules jest --watchAll=false --color",
    "test:watch": "NODE_OPTIONS=--experimental-vm-modules jest --watch",
    "format": "prettier -w ."
  },
  "devDependencies": {
    "@jest/globals": "^30.2.0",
    "@testing-library/dom": "^9.3.4",
    "@testing-library/jest-dom": "^6.9.1",
    "jest": "^29.7.0",
    "jest-environment-jsdom": "^29.7.0",
    "prettier": "^3.3.3",
    "serve": "^14.2.3"
  },
  "jest": {
    "testEnvironment": "jsdom",
    "setupFilesAfterEnv": [
      "<rootDir>/tests/setup.js"
    ],
    "roots": [
      "<rootDir>/tests"
    ],
    "moduleFileExtensions": [
      "js",
      "json"
    ],
    "transform": {}
  }
}
